mixin dialog(name, message, time, avatar, url = "#")
  a.message.mb-3(tabindex=0 href= url)
    .message-avatar
      img.avatar(src=avatar, alt="")
    .message-body
      .message-name-time
        p.message-name= name
        p.message-time= time
      p.message-text= message
